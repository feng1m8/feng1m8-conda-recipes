{% set name = "ismkl" %}
{% set version = "1.0" %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  number: 0
  noarch: generic

requirements:
  run:
    - libblas * *mkl

about:
  license: MIT
  summary: pinned mkl installation

extra:
  recipe-maintainers:
    - feng1m8

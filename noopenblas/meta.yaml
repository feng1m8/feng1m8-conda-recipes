{% set name = "noopenblas" %}
{% set version = "1.0" %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  number: 0
  noarch: generic

requirements:
  run_constrained:
    - libopenblas <0.0a0

about:
  license: MIT
  summary: prevents libopenblas installation

extra:
  recipe-maintainers:
    - feng1m8
